1. Phil to provide a write-up to Tom on Monday  describing the storage strategy and period for the battery data log.
2. Tom to turn this write up into a document that we share with Gold by Tuesday, if possible. Final approval by John.
3. John / Phil to share with Gilbert the Hithium specifications and the Chinese PCS interface standard (from Gold?).


notes


the aggregate method will sum up the rack measured variable into the dest.
If the src and dest are bitb , any rack bit being set will set the dest bit.
For rtos values the limits are used to set up the rtos (mcore) limits


